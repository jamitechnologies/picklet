version: 2

models:
  - name: stg_users
    description: "Staging view of users table"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
      - name: email
        description: "User email address"
      - name: created_at
        description: "Timestamp of creation"
